backend:
  name: git-gateway
  branch: main # Branch to update (optional; defaults to master)
media_folder: static/images
public_folder: /images
collections:
  - name: 'blog'
    label: 'Blog'
    folder: 'content/English/blog'
    create: true
    editor:
      preview: false
    fields:
      - { label: 'Title', name: 'title', widget: 'string' }
      - { label: 'Body', name: 'body', widget: 'markdown', required: false }
      - label: "Header"
        name: "Header"
        collapse: true
        widget: "object"
        fields:
          - {label: 'Header', name: 'heading', widget: 'string', required: false}
          - {label: 'Header', name: 'summary', widget: 'string', required: false}
          - {label: 'Header', name: 'featuredimage', widget: 'image', required: false}
          - {label: 'Header', name: 'Alternative Test', widget: 'string'}
          - {label: 'Header', name: 'featuredvideo', widget: 'videocode', required: false, hint: 'example 6R8Bn9SmVG8'}
